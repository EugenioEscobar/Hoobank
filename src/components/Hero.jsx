import styles from '../style';
import {discount, robot} from '../assets';

const Hero = () => {
    return (
        <section id='home' className='bg-white'>

        </section>)
}

export default Hero